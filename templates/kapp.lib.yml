#@ def kapp_cmd(config_dir):
#@   return "ytt -f " + config_dir
#@ end

#@ def kapp_task(input, kapp_config):
task: execute
config:
  platform: linux
  image_resource:
    type: registry-image
    source:
      repository: k14s/image
      tag: kbld-rand-1592942790507001000-1148625018150
  inputs:
  - name: #@ input
    path: repo
  outputs:
  - name: repo
  run:
    dir: repo
    path: sh
    args:
    - "-c"
    - #@ kapp_cmd(kapp_config.config_dir)
#@ end
